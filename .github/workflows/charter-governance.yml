name: Governance Check

on:
  pull_request:
    branches: [main, master]

permissions:
  contents: read
  pull-requests: write

jobs:
  governance:
    name: Charter
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate Commits
        run: pnpm exec charter validate --ci --format text

      - name: Drift Scan
        run: pnpm exec charter drift --ci --format text

      - name: Audit Report
        run: pnpm exec charter audit --format json > /tmp/audit.json
        if: always()
